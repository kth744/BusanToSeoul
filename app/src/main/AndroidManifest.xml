<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.busantoseoul.busan">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="서부인"
        android:theme="@style/AppTheme">

        <activity android:name=".Splash"></activity>
        <activity android:name=".TabController"></activity>
        <activity android:name=".FragmentTabBoardRead"></activity>

    </application>

</manifest>
